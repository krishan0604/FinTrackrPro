# Active profile (use 'prod' for production with real DB, 'default' for H2)
spring.profiles.active=${SPRING_PROFILE:default}

# JWT
app.jwt.secret=${JWT_SECRET:financedashboard2024supersecretkey256bits}
app.jwt.expiration=86400000

# Server
server.port=${PORT:8080}
